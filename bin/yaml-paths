#!/usr/bin/env python3
# pylint: disable=locally-disabled,invalid-name
"""
Returns zero or more YAML Paths indicating any values which match a given
search expression within source YAML/Compatible data.  Only array elements and
values in key-value pairs are searched.  For anchored value aliases, you can
choose whether to return only the original anchor value -- the default behavior
-- or to return it along with all of its aliases.

Copyright 2019 William W. Kimball, Jr. MBA MSIS
"""

# Implied Constants
MY_VERSION = "0.0.1"

def processcli():
    """Process command-line arguments."""

def validateargs(args, log):
    """Validate command-line arguments."""
    has_errors = False

def main():
    """Main code."""

if __name__ == "__main__":
    main()
